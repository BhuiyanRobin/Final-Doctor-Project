<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mobileappdevelop.simpledoctor">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Login" />
        <activity android:name=".Register" />
        <activity
            android:name=".DoctorsList"
            android:label="@string/title_activity_doctors_list"
            android:theme="@style/AppTheme" />
        <activity android:name=".AddDoctor" />
        <activity android:name=".ShowDoctorDetaile" />
        <activity android:name=".RecoveryPassword"></activity>
    </application>

</manifest>